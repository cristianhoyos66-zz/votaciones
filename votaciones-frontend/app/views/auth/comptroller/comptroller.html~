<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">
      Candidatos a Contralor      
    </h1>
  </div>  
</div>
<div class="row">
  <div class="col-lg-12">
    <div class="form-group col-md-12">
      <label for="name">
	Seleccione los estudiantes candidatos a contralor:
      </label>
      <ui-select theme="bootstrap"
		 multiple
		 ng-model="comptrollersVm.selectedStudent">
	<ui-select-match placeholder="Seleccione estudiantes">
          {{$item}}
	</ui-select-match>
	<ui-select-choices repeat="item in (comptrollersVm.students | filter: $select.search) track by item.id">
          <span ng-bind="item.name"></span>
	</ui-select-choices>
      </ui-select>
    </div>
  </div>
</div>

